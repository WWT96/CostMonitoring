python -m streamlit run app.py

本地运行说明：
- 配置入口：settings.json
- 本地数据库：cost_monitor_data.db
- 本地日志目录：logs/
- 系统设置页可维护数据路径、LLM Key / Base URL，并支持数据库压缩

推荐启动方式：
1. 激活项目虚拟环境
2. 运行 python -m streamlit run app.py
3. 在浏览器打开 http://localhost:8501
在加权时，建议将专家标注样本的权重设置为普通样本的 50-100 倍。这样随机森林在寻找“最小二乘法”最优解时，会像被吸铁石吸引一样贴近专家的结论。

您可以先从第一阶段的 Prompt 开始发给 Trae。 当它完成标注和存储功能后，我们再进行后续的智能化升级。