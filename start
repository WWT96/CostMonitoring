python -m streamlit run app.py

测算思路：


Model 16:23
这是一个非常深刻的问题！你直接问到了这套算法最核心的“秘密”：既然不让人去定标准，那算法自己是怎么决定“哪些人算一个圈子，这个圈子到底圈多少人”呢？
算法决定“邻居圈”的大小和人数，不靠猜，而是靠计算**“步长的突然变长”**。
我们可以通过一个**“排队测距离”**的生动例子来理解：
第一步：量一量每个人的“间距”
算法会把所有备件的价格从低到高排成一队（比如：5元、1000元、1020元、1050元、1100元、1500元、1550元）。
然后，算法去量每相邻两个人之间的距离（差价）：
5元 到 1000元，距离是 995元。
1000元 到 1020元，距离是 20元。
1020元 到 1050元，距离是 30元。
1100元 到 1500元，距离是 400元。
第二步：找出“正常间距”是多少
量完所有的距离后，算法会把这些距离放在一起，算出一个**“大多数人的平均间距”**（在数学上叫中位数间距）。
比如在上面的队伍中，大多数人的间距都在 20元到50元 左右。算法就会在心里记下：“在这个备件简称下，30元左右 才是邻居之间正常的距离。”
第三步：寻找“断层”（决定圈子的边界）
现在，算法开始从人最多的地方（比如 1000元附近）向两边数人，决定把谁圈进来。它的原则是：只要你和上一个人的距离，没有发生“离谱的暴增”，你就属于这个圈子。
往右数：1000 到 1020，距离 20，正常！圈进来。
继续数：1020 到 1050，距离 30，正常！圈进来。
遇到台阶：1100 到 1500，距离 400。
算法会进行智能判断：虽然 400 比 30 大，但 1500 后面还跟着一堆人（1550, 1560...），这代表 1500 不是孤立的。这种距离在数学上被判定为**“合理的阶梯”，于是把 1500 这一波人也并入圈子**。
往左数：从 1000 往左看，下一个是 5 元。距离是 995元！
算法会立刻拉响警报：“995 比正常间距（30元）大了 30 多倍！而且 5 元左边再也没有任何人了。”
算法判定：这是一个**“大断层”。圈子在这里强制切断**，5 元被无情地挡在圈外。
总结：圈子的人数是怎么定的？
所以，圈子里最终有 50 人还是 100 人，完全是由数据中“断层”出现在哪里决定的。
如果数据分布很均匀，没有大断层，算法就会画一个大圈子，把几乎所有人都包进来。
如果数据分布非常诡异，中间有巨大的鸿沟，算法就会在鸿沟处画线，形成小圈子。
这种“根据距离找断层”的方法，就彻底消除了人为规定“必须圈 90% 的人”或者“必须以 1.5 倍为界”的主观性。完全是数据自己决定的！


测算四个核心指标
一旦我们锁定了那个最大的“邻居圈”（比如 1000 元到 1550 元），剩下的数值就非常好算了：
1. 预测值（基准合理价）：主群体的“中心点”
怎么算的：我们在这个最大的“邻居圈”里，找到人站得最密集、最正中的那个点。
直白理解：如果 1000-1550 是最主流的定价范围，那么 1250 元 往往就是这个圈子的“灵魂核心”，它是最能代表这类备件真实身价的**“公允参考价”**。
2. 合理下限：圈子的“左边界”
怎么算的：在这个最大的“邻居圈”里，站得最靠左（价格最低）的那个正常人的位置。
直白理解：比如这圈人里最便宜的是 1000 元，那 1000 元 就是下限。比这个还低（比如那个落单的 5 元），就被挡在圈外，判定为异常。
3. 合理上限：圈子的“右边界”
怎么算的：在这个最大的“邻居圈”里，站得最靠右（价格最高）的那个正常人的位置。
直白理解：比如这圈人里最贵的是 1550 元，那 1550 元 就是上限。超过这个数（比如突然出现一个 5000 元的），也被判定为异常。
4. 偏离数值：你离“中心”有多远
怎么算的：实际价格 - 预测值（中心点）。
直白理解：那个 5 元的价格，距离 1250 元的中心点，足足差了 -1245 元。这就是它偏离的“绝对距离”。
5. 偏离比例：你离“中心”有多离谱
怎么算的：偏离数值 / 预测值（中心点）。
直白理解：用偏离的 1245 元除以基准价 1250 元，得出 -99.6%。这代表这个价格比正常中心价便宜了快 100%，这在逻辑上是非常离谱的。




在接通外部 API 后，为了实现从“被动接收”到“主动拉取”并完成系统集成，你主要需要修改以下 5 个文件：
1. .env (配置值层)
修改内容：填入外部系统给你的真实 URL 地址 和 授权 Token。
目的：确保程序有地方读取连接“外部世界”的钥匙。
2. config.py (配置定义层)
修改内容：增加对新环境变量（如 enterprise_api_url）的定义。
目的：让整个 Python 项目在代码层面能识别并引用 .env 里的新参数。
3. processor.py (数据逻辑层) —— 最核心的修改点
修改内容 1 (FIELD_MAP)：将外部 API 返回的英文/Java 字段名（如 item_code）与你的中文列名（物料编码）对应起来。
修改内容 2 (新增函数)：编写一个 fetch_data_from_enterprise() 函数，使用 requests 库去调用外部 API 并将 JSON 数据转化为 DataFrame。
4. app.py (界面层)
修改内容：在 UI 的“数据源设置”板块，增加一个 “从企业系统同步数据”的按钮 或 切换开关。
目的：让用户在界面上可以一键触发“拉取外部数据”的动作。
5. requirements.txt (环境依赖层)
修改内容：添加 requests 库（如果之前没加的话）。
目的：确保在部署到服务器时，系统能自动安装发送网络请求所需的工具。
总结你的操作顺序：
在 .env 填地址 -> 2. 在 config.py 申明变量 -> 3. 在 processor.py 写映射和拉取逻辑 -> 4. 在 app.py 做按钮显示。






第一阶段：数据扩容、反馈持久化与标注 UI 实现
目标：建立全量历史数据处理能力，并实现“人机协同”的标注存储机制。
复制以下 Prompt 发送给 Trae：
Task: [Phase 1] 建立全量历史数据分析引擎与专家反馈持久化机制。
1. 数据引擎升级 (processor.py)：

全量扫描：修改 detect_cost_anomalies 逻辑，停止使用“最新日期”截断。输入数据必须包含用户同步的所有历史记录。

唯一标识：为每条记录建立唯一主键（物料编码_工厂_日期_成本），确保标注的唯一性。

2. 建立反馈管理类 (LabelManager)：

在 processor.py 中新增 LabelManager 类。

持久化存储：在项目根目录创建 user_feedback.csv，记录用户手动标注的 正常 或 异常 标签。

功能函数：实现 save_label(key, status) 和 get_labels() 函数。

3. 异常监控 UI 增强 (app.py)：

手动标注按钮：在异常结果表格中，利用 Streamlit 的数据编辑器或 HTML 注入，为每行增加“标注为正常”的交互操作。

双模式开关：在异常监控页顶部增加切换开关：原始测算模式 vs 优化后测算（专家纠偏）模式。

状态显示：实时显示当前已由专家手动校准的记录总数。

4. 交付要求：请输出修改后的 processor.py 核心算法框架和 app.py 的交互逻辑，确保 user_feedback.csv 能够正常读写且不丢失。

第二阶段：实施策略 B（加权学习）与双模式计算

目标：实现“自学习”算法。当用户标注为“正常”时，通过增加样本权重来强迫模型“纠偏”。

复制以下 Prompt 发送给 Trae：

Task: [Phase 2] 实现“策略 B”加权自学习算法与双模式计算。

1. 优化后测算算法 (processor.py)：

实现策略 B (样本加权)：

在“优化后测算”模式下，读取 user_feedback.csv。

如果用户将某条历史记录标注为“正常”，在训练 RandomForestRegressor 时，给该样本分配极高的权重 (Sample Weight)。

目的：通过高权重强迫模型预测值（中轴线）向专家标注点偏移，而不再将其判定为异常，且不影响整体 
𝜎
σ
 参数。

计算指标：为标注过的记录在最终报表中增加“专家校准”标签。

2. 结果比对展示 (app.py)：

当用户选择“优化后测算”时，在统计图表上方对比显示：对比原始算法，专家干预后“异常率”下降了多少。

确保导出 Excel 功能支持导出“优化后”的最终判定结论。

第三阶段：Skills 技能书与 AutoResearch 棘轮迭代

目标：实现符合规范的技能书导出，以及基于“棘轮循环”的自动优化过程。

复制以下 Prompt 发送给 Trae：

Task: [Phase 3] 构建 Skills 技能引擎与基于“棘轮循环”的 AutoResearch 迭代。

1. Skills 技能引擎 (skills_engine.py)：

规范化定义：定义 JSON 格式的 Skills 结构，包含：备件简称、适用算法、数据结构分布描述、当前 σ 参数、偏置权重、成本合理区间边界。

人机读报告：编写导出函数，将上述 JSON 转换为人类可读的 Markdown/PDF 格式报告。

2. AutoResearch 棘轮迭代逻辑：

自动调参：支持用户选择迭代次数（5, 10, 20 次）。

迭代逻辑：系统在每次迭代中尝试微调 
𝜎
σ
 系数和偏置权重。

棘轮循环规则 (Strict Ratchet Loop)：

记录每次迭代的“准确率得分”（即模型预测与专家标注的一致性）。

严禁倒退：新参数必须优于当前最佳参数才能被采纳。

回滚机制：允许在出现计算错误时最多回滚一个版本，但绝不允许持续性能衰减。

3. 最终报表输出：

输出全量备件的深度审计报表，字段包含：物料编码、物料名称、备件简称、成本数值、原始结论、专家反馈、最终优化结论。

4. 交付要求：请确保 AutoResearch 运行过程中有进度条提示，且最终输出的 Skills 文件可直接作为系统的“知识资产”下载。

给您的技术专家建议：

关于“棘轮循环（Ratchet Loop）”：
我会让 Trae 在代码中建立一个 Best_State 变量。每次迭代后，系统会检查：新模型捕捉到的用户标注记录 / 总标注记录 的比例是否提高。如果没有提高，直接舍弃这组参数，绝对不更新 Skills。这就是所谓的“只能前进，不能后退”。

关于“策略 B”的强度：
在加权时，建议将专家标注样本的权重设置为普通样本的 50-100 倍。这样随机森林在寻找“最小二乘法”最优解时，会像被吸铁石吸引一样贴近专家的结论。

您可以先从第一阶段的 Prompt 开始发给 Trae。 当它完成标注和存储功能后，我们再进行后续的智能化升级。